class Palindrome():
    def palindrome():
        """define the 2 test character count the lengths of the test words use a while loop to compare the 1st (aka location zero) character with last character return the findings if it is a palindrome or not
        """
    testword1 = 'tacocat'
    testword2 = 'simplicity'
    testword3 = 'poop'
    testword4 = 12321
    testword5 = 1551
    testword6 = 14251
    j = len(testword2)
    #test for odd number, if yes then add one to find the middle value to stop the comparison index
    if (j%2)==0:
        i = len(testword2)
    else:
        i = len(testword2)+1
    k = 0
    l = int(j-1)
    m = int(i/2)
    def character comparison()
    while k < i:
        l > i
        if testword2[k] == testword2[l]:
            k +=1
            l -=1
        else:
            print("it is not a palindrome")
    print("it is a paildrome")
